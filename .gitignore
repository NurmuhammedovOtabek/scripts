node_modules/

# Secrets — never commit
.env
.env.*
!.env.example

# Build metadata (dist itself is committed on purpose)
*.tsbuildinfo

# OS / editor
.DS_Store

# Python virtualenv for the captcha solver (created per machine)
.venv/

# Runtime log file written by log-file.ts
/logs/
