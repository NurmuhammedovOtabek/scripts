import { HttpService } from '@nestjs/axios';
import { HtmlParserService } from '../html-parser/html-parser.service';
import { CaptchaSolverClient } from '../common/captcha-solver.client';
import { MemoryCacheService } from '../common/memory-cache.service';
import { NoProxyPool } from '../common/no-proxy.pool';
export declare class DavreestrService {
    private readonly httpService;
    private readonly htmlParser;
    private readonly solverClient;
    private readonly cacheManager;
    private readonly proxyPool;
    private readonly logger;
    private session;
    private userAgentIndex;
    private requestCount;
    private readonly MAX_REQUESTS;
    private readonly TIME_WINDOW;
    private windowStart;
    private readonly CACHE_TTL_MS;
    private readonly CACHE_GET_TIMEOUT_MS;
    constructor(httpService: HttpService, htmlParser: HtmlParserService, solverClient: CaptchaSolverClient, cacheManager: MemoryCacheService, proxyPool: NoProxyPool);
    private readonly DIRECT_TIMEOUT_MS;
    private readonly PROXY_TIMEOUT_MS;
    private get transport();
    isPartial(parsed: {
        totalCount?: number;
        cadNumbers?: string[];
        rowsSeen?: number;
    }): boolean;
    private cacheKeyForTin;
    private cacheKeyForCad;
    private cacheGet;
    private cacheSet;
    private getNextUserAgent;
    private getSessionUserAgent;
    initializeSession(forceDirect?: boolean): Promise<void>;
    private captchaImgUrl;
    fetchCaptcha(): Promise<Buffer>;
    private extractCaptchaUrl;
    private readonly MAX_SEARCH_RETRIES;
    searchByTin(tin: string, retryCount?: number): Promise<any>;
    searchByCadNumber(cadNumber: string, retryCount?: number): Promise<any>;
    private reloadCaptcha;
    private checkRateLimit;
    private readonly MAX_CAPTCHA_ATTEMPTS;
    solveCaptcha(): Promise<string>;
    private updateCookies;
    private extractCookieValue;
    private delay;
    clearSession(ok?: boolean): void;
}
